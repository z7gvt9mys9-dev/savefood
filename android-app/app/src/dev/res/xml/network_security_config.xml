<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- DEV FLAVOR ONLY. This file overrides src/main's strict HTTPS-only config
         for the `dev` product flavor, allowing cleartext to the local FastAPI
         backend reached over the emulator loopback (10.0.2.2) / localhost.
         The `prod` flavor never sees this file, so release builds stay HTTPS-only. -->
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
