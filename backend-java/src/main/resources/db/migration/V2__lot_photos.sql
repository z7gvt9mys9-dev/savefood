-- Несколько фотографий лота: JSON-массив URL в TEXT-колонке `photos`.
-- Первая фотография по-прежнему дублируется в старую колонку `photo` —
-- её читают карта волонтёра и старые клиенты (обратная совместимость).
ALTER TABLE lots ADD COLUMN IF NOT EXISTS photos TEXT;
