FROM golang:1.24-alpine AS build
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -ldflags="-s -w" -o /geows .

FROM alpine:3.20
RUN adduser -D -u 1000 app
USER app
COPY --from=build /geows /usr/local/bin/geows
EXPOSE 8001
CMD ["geows"]
